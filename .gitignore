__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv/
.venv-*/
venv/
*.log

# Secrets — never commit (defensive; none currently present)
.env
.env.*
.dev.vars
kaggle.json
*.pem
*.key
id_rsa*
candidates*.json
/kaggle_working/

# Competition SDK: vendored for reproducibility (MIT, see THIRD_PARTY_NOTICES.md).
# The working note pins its §4 code citations to comp/aicomp_sdk 3.1.2, so the
# sources must be published for those citations to resolve. Local run state and
# caches stay out.
comp/.aicomp/
comp/**/__pycache__/
refkernels/
/tmp/
poll_and_download.sh
*.zip

# Downloaded model weights (multi-GB GGUFs) — never commit
models/

# OS / tool-state junk
.DS_Store
.serena/
.playwright-mcp/
.worktrees/

# Competitor kernels (downloaded; not ours to commit — same as refkernels/)
refkernels_new/
